# 本地数据库覆盖配置
# 复制为 application-local.yml 并修改为你的 MySQL 连接（该文件不提交版本库）
# 启动时添加: --spring.profiles.active=dev,local
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ppm_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&nullCatalogMeansCurrent=true
    username: root
    password: fuqiuyang030828
