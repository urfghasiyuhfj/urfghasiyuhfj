server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
    location /api/ {
        proxy_pass http://ppm-backend:8080/;   # 后端服务名（docker-compose 中定义）
        proxy_set_header Host $host;
    }
}